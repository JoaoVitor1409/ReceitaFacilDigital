<header class="row">
    <div class="col-md-12">
        <h1>Crie sua conta aqui!</h1>
    </div>
</header>
<form action="/registrar" method="POST" class="row">
    <div class="row justify-content-center">
        <div class="col-md-6 row justify-content-between">
            <?php foreach ($this->view->options as $option) { ?>
                <div class="col-3">
                    <input type="radio" class="d-none" <?= $option['checked'] ? "checked" : "" ?> name="options" id="<?= $option['option'] ?>" value="<?= $option['value'] ?>">
                    <label class="btnChoice <?= $option['option'] ?>Btn <?= $option['checked'] ? "actived" : "" ?>" for="<?= $option['option'] ?>"><?= $option['valueFormated'] ?></label>
                </div>
            <?php } ?>
        </div>
    </div>


    <div class="allInputs">
        <!-- Inputs Generated by JS -->
    </div>


    <div class="row inputs inputPass">
        <div class="row justify-content-center align-items-center">
            <div class="col-md-8 inputArea">
                <div class="input-group">
                    <label for="password" class="input-group-text inputIcon passwordIcon">
                        <span class="material-icons-outlined">lock</span>
                    </label>
                    <input type="password" name="password" class="form-control passwordInput" id="password" placeholder="Senha" required>
                    <div class="viewPassDiv passwordIconView">
                        <div class="input-group-text d-flex justify-content-center align-items-center viewPass">
                            <span class="material-icons-outlined viewPassIcon">visibility</span>
                        </div>
                    </div>
                </div>
                <div class="errorDiv text-start d-none passwordError">
                    <p class="passwordErrorMsg error text-danger">Invalid</p>
                </div>
            </div>
        </div>
    </div>

    <div class="col-md-12 row">
        <div class="col-md-12 d-flex justify-content-center align-items-center">
            <button type="submit" class="btnAction">Criar sua conta</button>
        </div>
    </div>
</form>